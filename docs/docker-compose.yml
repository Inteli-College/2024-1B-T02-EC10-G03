services:
    docs:
        build:
            context: .
            dockerfile: Dockerfile.base
        command: sh -c "npm install && npm run dev"
        tty: true
        volumes:
            - .:/usr/src/app
        ports:
            - '4321:4321'
        environment:
            NODE_ENV: development
